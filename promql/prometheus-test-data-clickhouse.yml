# Same scrape targets as prometheus-test-data.yml, plus remote_write to ClickHouse.
# Start reference Prometheus with:
#   prometheus --config.file=prometheus-test-data-clickhouse.yml
#
# Prerequisites: ClickHouse published on the host (default 127.0.0.1:19093 → container :9093; see clickhouse-docker/).

global:
  scrape_interval: 5s

scrape_configs:
- job_name: 'demo'
  static_configs:
    - targets:
      - 'demo.promlabs.com:10000'
      - 'demo.promlabs.com:10001'
      - 'demo.promlabs.com:10002'

remote_write:
  - url: http://127.0.0.1:19093/write
